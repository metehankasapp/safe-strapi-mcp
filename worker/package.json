{
  "name": "safe-strapi-cloudflare-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply safe-strapi --local",
    "db:migrate:remote": "wrangler d1 migrations apply safe-strapi --remote"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "agents": "0.22.0",
    "zod": "4.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260908.1",
    "typescript": "^5.9.2",
    "wrangler": "4.129.1"
  }
}
